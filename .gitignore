# Environment
.env
.env.local

docs/


# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
*.egg-info/
.venv/

# Uploads (defect photos — local filesystem storage in v0)
uploads/

# Managed Kafka (Aiven): only ca.pem is tracked (public cert, no private key) —
# anything else dropped in certs/ (e.g. client key material) stays ignored.
certs/*
!certs/ca.pem

# Frontend
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

.vercel
.env*
